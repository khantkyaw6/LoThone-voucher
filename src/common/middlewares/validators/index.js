const { getPaginationValidator } = require("./getPagination.validator");
const { getParamIdValidator } = require("./getParamId.validator");

module.exports = { getPaginationValidator, getParamIdValidator };
